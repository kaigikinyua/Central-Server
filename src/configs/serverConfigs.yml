#Configuration file for the server
#types of server [relay, main, backup]
#RELAY SERVER
#combines a subset of P.O.S terminals example a shop with more
#than 1 pos terminal will report to a relay server

#Main SERVER
#It is the overall server and combines all the relay servers and makes 
#one central server

#BackUp SERVER
#Responsible for making daily backups of all the data generated by the system

server:
 type: central #[central,relay,backup]
 port: 8080
 mode: debug #[debug/prod]

database:
 username: app
 pass: falksdjflaksjdflkajsldkfa

backup:
 server: 109.168.266.125
 username: mainserver
 pass: fjaslkdfjalskdjflaksdjf
 frequency: 24 #number in hours if= 24 then it will backup every 24hrs for weekly=(24*7)=168 etc

logs:
 session: './data/logs/session/'
 notificationEmail: jamesBond@supermail.com
